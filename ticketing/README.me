UDEMY Microservices with NodeJS and React

Ticketing application from the course

working on the ticketing.com domain that needs to be configured for 127.0.0.1 in hosts file in Windows and /etc/hosts in MacOS/Linux for the Ingress-NGINX controller to resolve application requests to internal pods/services

--
Kubernetes
Creating a secret

kubectl create secret generic jwt-secret --from-literal=JWT_KEY=secureOrPrivateKey